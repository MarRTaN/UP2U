#pragma once

typedef enum StageName{
	ADVERTISING_STAGE = 0,
	DETECTING_STAGE = 1,
};

typedef enum Gender{
	MALE = 0,
	FEMALE = 1,
	UNDEFINED = 2,
};

typedef enum type{
	CHATBOX = 0,
	BUBBLE = 1,
};

